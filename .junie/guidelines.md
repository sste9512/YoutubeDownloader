# Project Guidelines
    

*Always use clean architecture for folder and class organization
*Always use SOLID principles
*Always use dependency injection
*Always try to wrap method types in Result or Optional 
*If possible, use span to be memory efficient
*Use ValueTask if a async method returns a value type
*When creating a async method, always give a parameter for cancellation token with default as value


